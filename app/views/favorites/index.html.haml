= render 'layouts/hedder'
#content
  .main
    = render 'layouts/page_top'
    .seach
      #info
        .movie--info-box
          %h2.heading 
            お気に入り
          - if user_signed_in?
            .movie--info-box
              %ul.movie-box
                - if @chapters
                  = render partial: 'layouts/movie-box', collection: @chapters, as: 'chapter'
                - else
                  %li お気に入りはまだ登録されていません。
          - else
            %p  ログインするとお気に入り機能を使用できます。